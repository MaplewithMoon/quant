# =============================================================================
#  依赖声明已迁移到 pyproject.toml —— 那边是唯一事实来源
#
#  日常使用（推荐）：
#      pip install -e ".[dev]"                 # 可编辑安装 + 开发工具
#      pip install -e ".[dev,talib,live]"      # 连 ta-lib / vnpy 一起装
#
#  需要 100% 复现已验证环境时，用锁文件装精确版本：
#      pip install -r requirements.lock.txt
#
#  为什么保留这个文件：很多工具/文档习惯性执行 `pip install -r requirements.txt`，
#  留着它可避免有人误装到一份过期清单。
# =============================================================================

-e .[dev]
