# 策略开发指南

> 给**第一次接触本项目**的人看的上手文档，也是**已经上手后查接口**的索引。
>
> - **想尽快跑通一个策略** → 只看 [第一章](#一十分钟跑通第一个策略) 和 [第二章](#二心智模型三层结构)。
> - **想查"某个东西怎么写"** → 直接跳 [第十章 接口速查表](#十接口速查表按我想做什么查)，
>   那里按"我想做什么"排列，每行给出函数、所在文件、一行示例。
> - **写之前请扫一眼** [第九章 红线](#九七条红线写策略前必读) —— 这个项目历史上
>   反复踩的是**前视**和**静默兜底**两类坑，代价都很大。

本文所有示例代码都在 **2026-09 用本项目真实数据跑通过**（文末说明验证方式）。

---

## 目录

| 章 | 内容 |
|---|---|
| [一](#一十分钟跑通第一个策略) | 10 分钟跑通第一个策略（完整可运行示例） |
| [二](#二心智模型三层结构) | 心智模型：三层结构 + 一条数据流 |
| [三](#三数据接口) | 数据接口：面板 / 单只股票 / PE、PB / 开高低收 |
| [四](#四选股股票池与行业) | 选股：股票池、按行业选、按财务选、指数成分 |
| [五](#五因子与信号) | 因子与信号：内置因子、自定义因子、动量计算、有效性评估 |
| [六](#六组合构建从打分到目标权重) | 组合构建：从打分到目标权重、加权、择时、风控 |
| [七](#七回测与交易函数) | 回测与交易函数：引擎、下单、制度约束、费用滑点 |
| [八](#八报告评估与交易记录) | 报告、评估与交易记录 |
| [九](#九七条红线写策略前必读) | **七条红线（写策略前必读）** |
| [十](#十接口速查表按我想做什么查) | **接口速查表（按"我想做什么"查）** |
| [十一](#十一常见问题) | 常见问题与报错 |
| [十二](#十二目录结构) | 目录结构 |

---

## 一、十分钟跑通第一个策略

### 1.1 环境（每个 PowerShell 都是新进程，PATH 必须手动刷新）

```powershell
$env:Path = [Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [Environment]::GetEnvironmentVariable('Path','User')
$env:PYTHONUTF8='1'; $env:PYTHONIOENCODING='utf-8'
cd D:\quant
python -c "import pandas; print(pandas.__version__)"     # 应为 3.x，Python 3.14
```

- **没有 scipy**（要 Spearman 就用 `factors/evaluation.py::_corr` 的"先排名再 Pearson"写法，
  别引入新依赖）。
- 下载数据才需要 `$env:TUSHARE_TOKEN = [Environment]::GetEnvironmentVariable('TUSHARE_TOKEN','Machine')`；
  **只读数据跑回测不需要**。

### 1.2 完整示例：医药行业内动量 + 估值选股，月度调仓

把下面整段存成 `my_strategy.py` 放在项目根目录，`python my_strategy.py` 即可：

```python
# -*- coding: utf-8 -*-
"""示例：沪深300 内、医药生物行业、动量+低估值打分，月度调仓 20 只等权"""
import sys
sys.path.insert(0, ".")

from universe import UniverseSpec, build_universe, universe_size
from database.industry import industry_pit_panel
from portfolio.construction import build_target_weights
from backtest.panel_data import load_price_panel
from backtest.multi_engine import PortfolioBacktestEngine
from analytics.trade_log import export_trade_log, audit_line

START, END = "2023-01-01", "2024-12-31"

# ① 面板：一次拿到所有横截面数据（index=交易日, columns=6 位股票代码）
#    limit=800 只留流动性最好的 800 只，探索阶段用来控制计算量；正式回测去掉
panel = load_price_panel(START, END, limit=800, cache_dir=".cache/panel_mine")

# ② 股票池：指数成分 + 上市天数 + 流动性 + ST/停牌 + 交易所（全部时点正确）
spec = UniverseSpec(index_code="000300.SH", min_listed_days=120, min_amount=5e7)
mask = build_universe(panel, spec)
print("股票池:", spec.describe(), "| 日均可用", f"{universe_size(mask).mean():.0f} 只")

# ③ 行业（PIT：按当时的申万归属，不是今天的归属）
ind = industry_pit_panel(panel["close"].index, panel["close"].columns)
INDUSTRY = "医药生物"

# ④ 打分：动量（复权价）+ 盈利收益率，各自在全市场排名后相加
close_adj = panel["close_adj"]              # 后复权价，算收益率必须用它
mom20 = close_adj / close_adj.shift(20) - 1
pe = panel["pe_ttm"]
ep = 1.0 / pe.where(pe > 0)                 # PE<=0 视为缺失
score = mom20.rank(axis=1, pct=True) + ep.rank(axis=1, pct=True)

# ⑤ 目标权重：只在医药行业内选，月度调仓，等权 20 只
tw = build_target_weights(score, mask & ind.eq(INDUSTRY),
                          n_hold=20, weighting="equal", rebalance="M")

# ⑥ 回测（T 日收盘决策 → T+1 开盘成交；A 股制度约束默认开启）
eng = PortfolioBacktestEngine(initial_capital=1_000_000, fill_timing="next_open")
res = eng.run(panel, tw, context={"index_code": "000300.SH",
                                  "industry_source": "pit"})

# ⑦ 报告 + 交易记录
print(res.render(title="我的策略"))
log = export_trade_log(res.trades, res.equity, None, outdir="results/mine",
                       tag="_mine", rejects=res.rejection_detail,
                       initial_cash=1_000_000, label="我的策略")
print(audit_line(log["audit"]))
```

跑完在 `results/mine/` 下会得到 5 个文件（交易记录 / 委托失败明细 / 收益分析 /
成交明细 CSV / 成交回合 CSV），终端会打印一份带口径说明和数据缺陷附注的报告。

### 1.3 这段代码在做什么

```
load_price_panel            ① 一个 dict，每个值都是「index=交易日 × columns=股票代码」的宽表
   ↓
build_universe              ② 同样形状的布尔宽表：True = 这天这只票可以买
   ↓
industry_pit_panel          ③ 同样形状的字符串宽表：值 = 行业名
   ↓
score（自己算）              ④ 同样形状的浮点宽表：值 = 打分，越大越看多
   ↓
build_target_weights        ⑤ 同样形状的浮点宽表：**调仓日**=目标权重，非调仓日=NaN
   ↓
PortfolioBacktestEngine.run ⑥ 撮合、扣费、T+1、涨跌停、停牌 → 结果对象
   ↓
res.render() / export_trade_log  ⑦ 报告与逐笔账单
```

**关键心智：全流程就是"同一形状的宽表一步步往下传"**。理解了这一点，
后面所有接口都只是"造一张什么形状的表"。

### 1.4 不想写代码？先跑 CLI 感受一下

```bash
# 沪深300 里按账面市值比（1/PB）选 30 只，月度调仓
python scripts/portfolio_backtest.py --index 000300.SH --factor bp \
    --n-hold 30 --rebalance M --start 2023-01-01 --end 2024-12-31

# 多因子合成 + 逆波动率加权 + 单票权重上限 5%
python scripts/portfolio_backtest.py --factor bp,mom_20,turn_20 --method icir_weight \
    --n-hold 30 --weighting inv_vol --max-weight 0.05

# 看全部参数
python scripts/portfolio_backtest.py --help
```

`--factor` 可用的名字：`python -c "from factors import list_factors; print(list_factors())"`。

---

## 二、心智模型：三层结构

```
┌─ ① 数据层  database/          db/frozen（只读原始）→ db/cleaned（派生）
│             factors/panel.py   load_panel()：把 7 万+ parquet 变成内存宽表
│
├─ ② 研究层  universe/          股票池（指数成分 + 过滤，时点正确）
│             factors/          因子计算 + 有效性评估（IC / 分层 / 衰减）
│             portfolio/        从打分到目标权重（选股 + 加权 + 调仓频率）
│
└─ ③ 回测层  backtest/          组合撮合引擎（T+1 / 涨跌停 / 停牌 / 整手）
              execution/        费用、滑点、冲击模型、制度约束
              analytics/        绩效、归因、风格、报告、交易记录
```

**一条最重要的约定：策略只输出"目标权重宽表"，永远不直接下单。**

- 为什么：资金分配、风控拦截、成交约束都要在一处统一处理；策略各自下单必然失控。
- 所以你在策略里**看不到** `buy()` / `sell()` 这类函数 —— 你给的是"我想持有这些票、
  各占多少比例"，引擎负责把它变成股数、价格和成交记录。
- 想写聚宽那种"逐笔下单"风格的策略也可以，走 `joinquant/` 兼容层
  （见 [7.6](#76-想写聚宽风格的策略joinquant-兼容层)）。

---

## 三、数据接口

### 3.1 推荐入口：面板（一次拿全市场）

```python
from factors import load_panel

panel = load_panel("2023-01-01", "2024-12-31",
                   codes=None,        # None=全部；给列表则只加载这些股票
                   with_valuation=True,  # 附带 PE/PB/市值/换手
                   adjust=True,       # 附带复权价（列名带 _adj）
                   limit=None)        # 只留日均成交额前 N 只（探索阶段提速）
```

返回 `dict[str, DataFrame]`，**每个 DataFrame 都是 index=交易日, columns=6 位股票代码**：

| 键 | 含义 | 单位/说明 |
|---|---|---|
| `open` `high` `low` `close` | **不复权**真实价 | 元。回测撮合必须用这个 |
| `pre_close` | 前收盘（不复权） | 元 |
| `close_adj` `open_adj` `high_adj` `low_adj` | **后复权**价（× `adj_factor`） | 算收益率、算动量必须用这个 |
| `volume` `amount` | 成交量 / 成交额 | 股 / 元 |
| `pct_chg` | 当日涨跌幅 | **百分数**（`-0.55` 表示 −0.55%） |
| `total_mv` `circ_mv` | 总市值 / 流通市值 | **万元**（tushare 原样） |
| `pe_ttm` `pb` `ps_ttm` `pe` `ps` | 估值 | 倍；亏损股可能为负或 NaN |
| `turnover_rate` | 换手率 | 百分数 |

回测还需要涨跌停与停牌，用 `backtest.panel_data.load_price_panel`（= `load_panel`
+ 下面三个键）：

| 键 | 含义 |
|---|---|
| `limit_up` `limit_down` | 当日涨停 / 跌停价。**NaN 表示"没有涨跌幅限制"**（新股上市初期、复牌首日），不是"数据缺失" |
| `suspended` | True = 当日停牌，不可交易 |
| `adj_factor` | 复权因子 |

```python
from backtest.panel_data import load_price_panel
panel = load_price_panel(START, END, cache_dir=".cache/panel_mine")   # 加缓存
```

> **面板缓存**：`cache_dir` 会把结果落盘，下次同区间 <10 秒。缓存里存了**数据指纹**，
> 底层数据一变自动作废 —— 不会静默用旧数据。首次加载全市场十年约 8 分钟，
> 所以调参时一定要用缓存。

### 3.2 取单只股票

```python
from database.loader import (load_daily, load_daily_qfq, load_raw_daily,
                             load_valuation, load_limit_price, load_trading_status)

load_raw_daily("600519", "2024-01-01", "2024-12-31")
#   → code, trade_date, open, high, low, close, pre_close, change, pct_chg, volume, amount
#     **不复权**真实价（推荐用这个当"价格"）

load_daily("600519", "2024-01-01", "2024-12-31", adjust="qfq")
#   → 同上，但 open/high/low/close/**pre_close** 全部**前复权**（最新价=真实价）
#     ⚠️ adjust="" 则不复权。默认是 "qfq"，别拿它当成交价用

load_valuation("600519", "2024-01-01", "2024-12-31")
#   → trade_date, pe, pe_ttm, pb, ps, ps_ttm, total_mv, circ_mv, turnover_rate, code

load_limit_price("600519", "2024-01-01", "2024-12-31")
#   → code, trade_date, pre_close, limit_up, limit_down

load_trading_status("600519", "2024-01-01", "2024-12-31")
#   → trade_date, limit_up, limit_down, suspended   ← 回测需要的"能否交易"
```

> ⚠️ **`load_daily_qfq()` 这个名字是历史遗留，它返回的是「不复权原始价」**
> （读 `frozen/daily_raw`）。要前复权请用 `load_daily(adjust="qfq")`。
> 名字有歧义，读代码时容易看错。

> ⚠️ **三种复权口径别混用**（这是最容易算错收益率的地方）：
>
> | 口径 | 从哪拿 | 用途 |
> |---|---|---|
> | 不复权 | `panel["close"]`、`load_raw_daily`、`load_daily(adjust="")` | **成交价**、涨跌停比较、盘口 |
> | 后复权 | `panel["close_adj"]` | 算收益率/动量/波动（**推荐**） |
> | 前复权 | `load_daily(adjust="qfq")` | 看图、和行情软件对照 |
>
> 收益率用后复权或前复权**都正确**（只差一个常数比例）；**只有不复权价能当价格**。
> 把后复权价当成交价会让回测收益虚高几十倍。

### 3.3 PE / PB / 市值 / 换手

三个层次，按需要选：

```python
# ① 横截面面板（最常用，一次拿全部股票）
pe, pb = panel["pe_ttm"], panel["pb"]
ep = 1.0 / pe.where(pe > 0)          # 盈利收益率：PE<=0 视为缺失
bp = 1.0 / pb.where(pb > 0)          # 账面市值比
mv_yi = panel["total_mv"] / 1e4      # 万元 → 亿元

# ② 单只股票
v = load_valuation("600519", "2024-01-01", "2024-12-31")[["trade_date", "pe_ttm", "pb"]]

# ③ 注册成因子（之后可用 factor_eval.py 评估 IC）
from factors import get_factor
score = get_factor("bp").compute(panel)      # 等价于 1/PB
```

### 3.4 开盘 / 最高 / 最低 / 收盘

```python
panel["open"]; panel["high"]; panel["low"]; panel["close"]      # 不复权
panel["open_adj"]; panel["high_adj"]; panel["low_adj"]; panel["close_adj"]  # 后复权
```

日内振幅、真实波幅等用**后复权**价算：

```python
amp = (panel["high_adj"] - panel["low_adj"]) / panel["pre_close"].replace(0, float("nan"))
```

---

## 四、选股：股票池与行业

### 4.1 股票池：**一定要用 `build_universe`**

```python
from universe import UniverseSpec, build_universe, universe_size

spec = UniverseSpec(
    index_code="000300.SH",   # None = 全市场
    min_listed_days=120,      # 剔除次新股（上市不足 N 个交易日）
    min_amount=5e7,           # 日均成交额下限（元），默认窗口 20 日
    exclude_st=True,          # 剔除 ST
    exclude_suspended=True,   # 剔除停牌
    use_master=True,          # 证券主表三层过滤（交易所归属/已上市/未退市）
    exchanges=("SSE", "SZSE"),# 默认只保留沪深两市
    top_n=0,                  # 按流动性取前 N（0=不限）
)
mask = build_universe(panel, spec)     # 布尔宽表，与 panel["close"] 同形状
print(spec.describe())                 # 一行说明这条池子是怎么筛的
print(universe_size(mask).describe())  # 每日可选股票数
```

**为什么不能自己写 `close.notna()` 之类的土办法**（这是本项目踩得最狠的一类坑）：

| 土办法 | 看起来像 | 实际是 |
|---|---|---|
| 用当前 `stock_basic.industry` 做历史行业筛选 | "行业归属是慢变量" | **前视**（1,646 只股票换过行业） |
| 用代码前缀排除北交所/新三板 | "保守，宁可少交易" | 把前视从涨跌停挪到了**池子成员资格** |
| 用 `name` 含"退"判断退市 | "大概能猜出来" | 既漏（改名退市）又错（退市整理期仍可交易） |
| 用 `close.notna()` 当可交易判据 | "没行情自然不能交易" | 表达不了交易所归属，且掩盖幸存者偏差 |

`build_universe` 里的三层过滤全是**日期比较**，没有一处用到"今天的状态"：

```python
master[(master['exchange'].isin(['SSE','SZSE']))              # ① 交易所归属
       & (master['list_date'] <= t)                            # ② 已上市
       & (master['delist_date'].isna() | (master['delist_date'] > t))]  # ③ 未退市
```

主表查不到的代码一律视为不可交易 ——「不知道」不等于「可以交易」。

### 4.2 按行业选股（PIT，时点正确）

```python
from database.industry import (industry_pit_panel, industry_at,
                               load_sw_member, industry_intervals)

# 面板形式（推荐，一次拿全部）
ind = industry_pit_panel(panel["close"].index, panel["close"].columns)
#   → index=交易日, columns=代码, 值=申万一级行业名（31 个 + "未分类"）

mask_med = mask & ind.eq("医药生物")          # 只用医药生物

# 单只 / 单日
industry_at("600519", "2024-06-28")          # -> '食品饮料'（按当时的 PIT 归属）
industry_at("000001", "2015-06-30")          # -> '银行'

# 行业清单与成分
load_sw_member()      # l1_name / l2_name / l3_name / ts_code / in_date / out_date
industry_intervals()  # {代码: [(起, 止, 行业名), ...]}，已按 carry_forward 拼好
```

- `industry_pit_panel` 的 `attrs` 里有 `pit_coverage` / `fallback_ratio`，
  告诉你有多少行业是兜底猜的，写结论时可以引用。
- **不要**用 `load_industry_map()["industry"]` 做历史行业筛选 —— 那是**当前快照**，
  是前视（缺陷 B7）。它只适合"展示名称"。

### 4.3 按财务/估值筛股

```python
# 直接在宽表上做布尔运算，最后和 mask 相乘
# ⚠️ 面板里的字段都是 **DataFrame（多股票）**，没有 Series.between()，
#    区间筛选要写成两个比较再 & 起来
ok = mask & (panel["pe_ttm"] > 0) & (panel["pe_ttm"] < 30) & (panel["pb"] > 0) \
     & (panel["total_mv"] > 3e5)        # total_mv 单位万元 → >30 亿
```

要**时点正确的财报字段**（净利润 / 营收 / ROE / 毛利率 / SUE，按公告日对齐）用：

```python
from factors.fundamental import FundamentalData

fd = FundamentalData.load()                       # 读三大报表并算出全部因子
wide = fd.align("roe_ttm", panel["close"].index, panel["close"].columns)
#     → 日频宽表；对齐键是 effective = ann_date + lag_days（默认 1 天），不是 end_date
many = fd.align_many(["roe_ttm", "np_yoy", "gross_margin"],
                     panel["close"].index, panel["close"].columns)
print(list(fd.reports.columns))                   # 看看有哪些因子可用
```

> ⚠️ 所有财务数据必须按 **`ann_date`（公告日）** 对齐。用 `end_date`（报告期）
> 会让财报在公告前就生效 —— 典型前视。`FundamentalData.align` 已按公告日 + lag 对齐，
> 并对同一报告期只认**最早**那次公告（as-reported，杜绝事后修正值提前生效）。

### 4.4 指数成分

```python
from universe import index_member_panel, index_weight_panel, load_index_members

mem = load_index_members("000300.SH")                     # 历史快照长表
mem_panel = index_member_panel("000300.SH", dates, codes) # 布尔宽表（按日）
w = index_weight_panel("000300.SH", dates, codes)         # 权重宽表（当基准用）
```

`build_universe(index_code=...)` 内部走的就是这个，**一般不用自己调**。

---

## 五、因子与信号

### 5.1 内置因子（18 个，`factors/library.py`）

```python
from factors import list_factors, get_factor
print(list_factors())          # 因子名 / 类别 / 方向 / 说明
f = get_factor("mom_20")
score = f.compute(panel)       # → 宽表
```

| 类别 | 因子 | 方向 |
|---|---|---|
| 估值 | `ep`(1/PE) `bp`(1/PB) `sp`(1/PS) | 越大越好 |
| 动量反转 | `mom_20` `mom_60` `mom_120` `rev_5` `rev_20` | 越大越好 |
| 波动风险 | `vol_20` `vol_60` `amp_20` `max_ret_20` `skew_20` | 越小越好 |
| 流动性 | `turn_20`（越小越好） `illiq_20`（越大越好） | — |
| 规模 | `size` = ln(总市值) | 越小越好 |
| 量价 | `vol_ratio_20`（越大越好） `price_volume_corr_20`（越小越好） | — |

### 5.2 自己写因子

```python
from factors.base import register

@register("my_mom_vol", "20 日动量 / 20 日波动（风险调整动量）",
          direction=1, category="自定义")
def my_mom_vol(panel):
    c = panel["close_adj"]                  # 算收益率必须用后复权价
    ret = c.pct_change(fill_method=None)
    return (c / c.shift(20) - 1) / ret.rolling(20).std()

# 注册后就能用评估脚本和 get_factor：
#   python scripts/factor_eval.py --factor my_mom_vol --start ... --end ...
```

**约束**：只用当日及以前的数据（`shift` / `rolling` 只能是正向窗口）；
返回宽表；不要改 `panel`。

### 5.3 手写动量 / 波动 / 换手（不想注册因子时）

```python
c = panel["close_adj"]
mom_20  = c / c.shift(20) - 1
mom_120 = c / c.shift(120) - 1                       # 半年动量
vol_20  = c.pct_change(fill_method=None).rolling(20).std()
turn_20 = panel["turnover_rate"].rolling(20).mean()
rsi_like = (c.diff().clip(lower=0).rolling(14).sum()
            / c.diff().abs().rolling(14).sum())      # 0~1 的相对强弱
```

### 5.4 因子好不好用：先评估，再上策略

```python
from factors import evaluate, get_factor

rep = evaluate(get_factor("mom_20"), panel, periods=1, n_quantiles=5)
print(rep.summary())         # 含 IC 均值 / t 值 / 分层收益 / 单调性 / 换手 / 衰减
```

CLI 版本：

```bash
python scripts/factor_eval.py --list
python scripts/factor_eval.py --factor mom_20,bp --start 2020-01-01 --end 2024-12-31
```

**门禁**（`factors/gate.py`）：四个**互相独立**的条件
（|IC| ≥ 0.02、|t| ≥ 2、单调性 ≥ 0.5、样本量），任一不过就不该拿去跑策略。
`analytics/result_report.py` 会把门禁状态打在报告头部。

> 这个项目已经因为数据错误得出过错误结论。**证伪的结论和证实的一样有价值**，
> 别用"显著"这种词掩盖 `|IC| = 0.05`、`t = 2.2` 这种擦线值。

### 5.5 合成打分

```python
from factors.composite import composite_score, correlation_matrix

correlation_matrix(panel, ["mom_20", "bp", "vol_20"], mask)   # 先看相关性
score = composite_score(panel, ["mom_20", "bp"], method="equal", panel_mask=mask)
#   method: 'equal' | 'ic_weight' | 'icir_weight'
```

`composite_score` 会**先按 `panel_mask` 做横截面标准化再合成**，
并自动处理方向（`direction=-1` 的因子会被取负）。

---

## 六、组合构建：从打分到目标权重

```python
from portfolio.construction import build_target_weights

tw = build_target_weights(
    score,                    # 打分宽表（越大越看多）
    mask,                     # 股票池布尔宽表
    n_hold=30,                # 持股数（quantile>0 时忽略）
    weighting="equal",        # 'equal' | 'market_cap' | 'inv_vol'
    mv=panel["total_mv"],     # weighting='market_cap' 时需要
    vol=vol_60,               # weighting='inv_vol' 时需要
    rebalance="M",            # 'D'/'W'/'M' 或整数（每 N 个交易日）
    max_weight=0.05,          # 单票权重上限（0=不限）
    quantile=0.0,             # >0 时按分位选股，如前 20% 填 0.2
)
```

返回值：**调仓日 = 归一化权重，非调仓日 = NaN**。
引擎靠 NaN 判断"今天不调仓、持仓自然漂移"，全 0 会被理解成"清仓"。

常用辅助：

```python
from portfolio.construction import (rebalance_dates, turnover_of_weights,
                                    select_top_n, weights_equal, cap_weights,
                                    equal_weight_returns)

rebalance_dates(panel["close"].index, "M")   # 每月第一个交易日
turnover_of_weights(tw)                       # 每次调仓的换手（|Δw|/2）
equal_weight_returns(panel, mask, "M")        # 等权基准的日收益（解析解，不跑撮合）
```

### 6.1 择时（控制总仓位）

```python
from factors.market import exposure_from_signal

exp = exposure_from_signal(sig,                  # 你的择时信号（index=交易日）
                           low_q=0.3, high_q=0.7,   # 信号分位低于/高于各自阈值时
                           min_exposure=0.3,        #   仓位取 min/max，中间线性
                           max_exposure=1.0,
                           expanding=True)          # 扩张窗口算分位（默认，实盘可复现）
res = eng.run(panel, tw, exposure=exp)           # 只在**调仓日**按 exp 缩放目标权重
```

> `expanding=True` 很关键：分位阈值只用 `[起点, t]` 的数据算。
> 用**全样本**分位就是前视 —— 2018 年的信号会"知道"2025 年的分布。

这样做的好处：择时只通过"这次调仓多买还是少买"生效，**调仓日之间不因为信号变化
临时加减仓** —— 否则会产生大量无谓交易，还容易把信号噪声当成调仓理由。

### 6.2 风控（组合层，只降风险）

```python
from risk import build_risk_manager

cfg = {"max_drawdown_pct": 0.20,   # 回撤超过 20% 开始降仓
       "start_pct": 0.10,          # 从 10% 回撤开始线性降
       "min_exposure": 0.0,        # 最多降到 0 仓
       "max_weight": 0.10,         # 单票上限 10%
       "max_turnover": 0.50,       # 单次调仓换手上限
       "max_gross": 1.0}           # 总仓位上限
rm = build_risk_manager(cfg)
res = eng.run(panel, tw, risk_manager=rm)
print(res.risk_events)             # 每次触发都有留痕
```

两条硬约束（新建规则时请遵守）：

1. **只能降风险**：`PortfolioRiskManager` 最后统一 clip，规则写错方向也放不大仓位。
2. **触发要留痕**：写进 `risk_events`，否则事后看不出"这次减仓是策略决定的还是风控砍的"。

> ⚠️ `PortfolioRiskManager` 有 `_prev_weights` 内部状态。**跨回测段复用一个实例
> 会让状态串味**（测试集第一次调仓会拿训练集末尾的持仓当上期持仓）——
> 传配置、每段现建，或调 `rm.reset()`。

---

## 七、回测与交易函数

### 7.1 引擎

```python
from backtest.multi_engine import PortfolioBacktestEngine

eng = PortfolioBacktestEngine(
    initial_capital=1_000_000,
    commission=0.0001,        # 佣金（单边）
    min_commission=5.0,       # 最低佣金
    stamp_duty=0.0005,        # 印花税（卖出）
    transfer_fee=0.00001,     # 过户费
    lot_size=100,             # 一手
    slippage=0.001,           # 固定滑点（单边）
    slippage_model=None,      # 或 execution.impact.build_model("sqrt", k=0.1)
    max_participation=0.10,   # 单笔最多吃掉当日成交量的比例
    fill_timing="next_open",  # 'next_open'（默认，可实现）| 'same_close'（上界）
    market_rules=None,        # None = 启用 A 股制度约束（推荐）
)
res = eng.run(panel, tw, exposure=None, risk_manager=None,
              audit_lookahead=True, context={...})
```

**成交时点**：
- `next_open`（默认）：T 日收盘算权重 → **T+1 开盘**成交。**可实现，是主结果**。
- `same_close`：T 日收盘算权重并按**同一收盘价**成交。是**上界**，实盘不可复现
  （收盘价要收盘后才知道）。`backtest/fill_compare.py` 会自动跑两版并给一句话判读。

### 7.2 交易函数在哪？

本项目的策略不直接下单（见第二章）。真要手动撮合（写自定义引擎、做压力测试）时：

```python
from portfolio.portfolio import Portfolio
from execution.broker import SimulatedBroker
from execution.market_rules import MarketRules
from execution.order import Order, OrderSide, OrderType

pf = Portfolio(1_000_000)
broker = SimulatedBroker(slippage=0.001, commission=0.0001, min_commission=5.0,
                         stamp_duty=0.0005, transfer_fee=0.00001, lot_size=100)
rules = MarketRules()                      # 默认启用

pf.new_day()                               # 每个交易日必须先调（解锁 T+1）
size = broker.max_affordable_size(pf.cash, market_price=10.0, volume=1e6)  # 按钱算能买多少
ok, why = rules.check_buy(bar, ref_price=10.0)      # bar 是 dict：见下
if ok:
    fees = broker.fees_of(size * 10.0, OrderSide.BUY)
    pf.buy("600519", size, 10.0, fees=fees)

pf.new_day()                               # 第二天：T+1 解锁后才可卖
pf.sell("600519", 500, 12.0, fees=broker.fees_of(500 * 12.0, OrderSide.SELL))
```

> 上面两次 `pf.new_day()` 不是多余的：**当日买入当日卖会被直接拒绝**
> （`可卖不足（T+1）`）。这就是制度约束在起作用，不是 bug。

`bar` 的字段（`check_buy` / `check_sell` 都吃它）：

```python
bar = {"open": ..., "high": ..., "low": ..., "close": ...,
       "limit_up": ...,      # NaN = 无涨跌幅限制（不是"没有限制"的反面）
       "limit_down": ...,
       "suspended": False}
```

**注意顺序**：`pf.new_day()` 必须在每天开盘前调用，否则 T+1 永远不解锁。

### 7.3 制度约束（默认开启，不要关）

`MarketRules` 负责：T+1（当日买入不可卖）、涨停买不进 / 跌停卖不掉、
停牌不可交易、整手（100 股）。

被拦下的委托会记在 `res.rejections`（次数）与 `res.rejection_detail`（逐笔明细，
含日期/代码/方向/数量/价格/原因）。

> 「封板判完全不可成交」是一个**保守假设**（真实排队可能成交一部分），
> 所以主结果是收益的**下界**。`res.attempted_turnover` 是"想成交的总金额"，
> 用来度量这个假设影响多大。

### 7.4 费用、滑点、冲击模型

```python
from execution.impact import build_model
# 'none'（零冲击）| 'fixed'（固定滑点）| 'sqrt'（平方根冲击，需配合 --capital 做容量分析）
broker = SimulatedBroker(slippage_model=build_model("sqrt", k=0.1))
```

**新增回测脚本时必须显式设置滑点与冲击模型**。零滑点会让结果偏乐观，
还会掩盖容量问题（资金调大也没反应）。

CLI 脚本统一用 `execution/setup.py` 提供的参数组：

```python
from execution.setup import add_execution_args, build_execution, describe_execution
add_execution_args(ap)                    # --fill-timing --slippage --impact-model ...
engine_kwargs = build_execution(args)     # → dict，喂给引擎
```

### 7.5 结果对象 `res` 里有什么

```python
res.equity              # pd.Series  逐日总资产
res.trades              # DataFrame  成交明细（timestamp/code/action/size/price/fee/pnl/balance）
res.holdings            # DataFrame  逐日持仓权重
res.metrics             # Metrics    总收益/年化/波动/夏普/回撤/胜率/交易笔数
res.rejections          # dict       被拦委托分类计数
res.rejection_detail    # list       被拦委托逐笔明细
res.ledger_gap          # float      账目差额，**应=0**（不为 0 说明账本有 bug）
res.lookahead_report    # str        成交时点前视自检
res.risk_events         # list       风控触发记录
res.defects             # list       自动匹配到的已知数据缺陷
res.gate                # dict       因子门禁结果
res.render(title=...)   # str        统一格式的完整报告
```

### 7.6 想写聚宽风格的策略（`joinquant/` 兼容层）

如果你更习惯聚宽的 API（`order_target_value` / `get_fundamentals` / `run_daily`），
可以直接写聚宽代码在本项目里跑：

```python
from joinquant.api import run_strategy
res = run_strategy(my_module, panel, "2024-01-01", "2025-12-31",
                   initial_cash=100_000, fill="auto")
```

- 现有范例：`strategies/jq/guojiu_smallcap.py`、`scripts/jq_backtest.py`。
- 兼容层覆盖约 40 个聚宽 API；**已知近似与差异**见 `docs/聚宽策略移植报告.md`。
- ⚠️ 兼容层的**夏普口径**与聚宽不同（我们用 rf=0 的算术年化，聚宽用
  `(几何年化−4%)/波动率`），对比时别直接并排看。

---

## 八、报告、评估与交易记录

### 8.1 标准报告

```python
print(res.render(title="我的策略"))
```

报告分五块，**头部先声明口径再给数字**：

```
  成交时点 : next_open（T日收盘决策 → T+1开盘成交）：**可实现口径，主结果**
  ⚠ 因子门禁 : ...          ← 没跑样本外检查时会明确警告
  ⚠ 已知缺陷 : 本次触及 3 项  ← 自动从 database/defects.py 匹配
  ── 一、绩效
  ── 二、已知数据缺陷附注（每条带"影响哪段/已做什么/还缺什么"）
  ── 三、前视自检（成交不早于决策）
```

⚠️ **数据缺陷是自动匹配的，不要绕过**：遇到"数据本身不对但代码没错"的区间，
登记到 `database/defects.py`（唯一登记处），回测报告会自动附注。

### 8.2 绩效指标

```python
from analytics.performance import performance_summary, drawdown_info, to_returns

summ = performance_summary(res.equity, bench)   # bench 可为 None
summ["annual_return"], summ["sharpe_ratio"], summ["max_drawdown"], summ["information_ratio"]
dd = drawdown_info(res.equity); print(dd.max_drawdown, dd.peak_date, dd.trough_date)
```

### 8.3 风格归因 / 残差 alpha

```python
from analytics.style import build_style_panels, residual_alpha, style_section
```

风格（市值/估值/动量…）暴露解释掉的收益不算 alpha，**残差 alpha 才是主口径**。
只有风格敞口、没有残差 alpha 的策略，本质是一个便宜的 smart-beta。

### 8.4 交易记录 / 收益分析导出（对齐聚宽那两份文本）

```python
from analytics.trade_log import export_trade_log, audit_line

out = export_trade_log(res.trades, res.equity, bench,
                       outdir="results/mine", tag="_mine",
                       rejects=res.rejection_detail,
                       initial_cash=1_000_000, label="我的策略")
print(audit_line(out["audit"]))
```

产出 5 个文件：`交易记录*.txt`（与聚宽同列同序）、`委托失败明细*.txt`、
`收益分析*.txt`（**同时给本项目口径与聚宽口径的夏普/索提诺**）、
`成交明细*.csv`、`成交回合*.csv`（FIFO 配对：持仓天数/毛利/费用/净利）。

`audit_trades` 会检查**只靠成交表就能查出的硬错误**：现金变负、卖超（持仓为负）、
非整手、成交日不在权益曲线的交易日上。**这几条不通过时，任何逐笔归因都不成立。**

---

## 九、七条红线（写策略前必读）

### 1. 任何"用今天决定历史"的做法都是前视

| 错误做法 | 看起来像 | 实际是 |
|---|---|---|
| 用当前行业快照做历史行业中性化 | "行业是慢变量" | 前视 |
| 用代码前缀排除北交所 | "保守" | 前视换了地方 |
| 用 `name` 含"退"判断退市 | "猜得出来" | 既漏又错 |
| 用 `end_date` 对齐财报 | "报告期就是那天" | 前视（还没公告） |
| `shift(-1)` / `rolling` 带负窗口 | "算趋势" | 用了未来数据 |

**地基只有一种：时点正确的证券主表**（`database/master.py`）+ 公告日对齐。

### 2. 算收益率用复权价，当价格用不复权价

`panel["close_adj"]` 算收益率；`panel["close"]` 当成交价。混用会让收益虚高几十倍。

### 3. 「我们不知道」≠「没有限制」

涨跌停价为 **NaN** 表示"该日无涨跌幅限制"（新股上市初期、复牌首日），
**不是**"数据缺失可以忽略"。处理方式是不交易这些 (股票, 日期)，而不是随便交易。

### 4. 静默兜底比报错危险得多

`X.get(k, default)` 里那个 default 若是"另一个看起来合理的列/值"，就会造出
**跑得通但结果没意义**的回测。本项目已经因此吃过三次亏（见
`docs/已知问题与待办汇总.md` 2.22）。**宁可返回 NaN 让调用方看见"这里没数据"。**

### 5. 守卫要判"列存不存在"，不能只判 `.empty`

loader 在无数据时可能返回**缺列**的对象，`.empty` 未必为 True，
随后 `merge(..., on="trade_date")` 直接 KeyError。CI 里炸过。

### 6. 合成测试不要隐式依赖 `db/`

CI 里没有 `db/`。凡是合成用例里要用日历/主表的，走**参数注入**：
`apply_limit_prices(..., cal=...)`、`build_universe(..., master=...)`。

### 7. 回测脚本必须显式声明滑点与冲击模型

零滑点偏乐观且掩盖容量问题；冲击成本走 `execution/impact.py`，
用 `--capital` 三档（100 万 / 1000 万 / 1 亿）做容量分析。

---

## 十、接口速查表（按"我想做什么"查）

### 10.1 数据

| 我想…… | 用什么 | 在哪 | 一行示例 |
|---|---|---|---|
| 拿全市场日线面板 | `load_panel` | `factors/panel.py` | `p = load_panel(s, e, limit=800)` |
| 拿面板 + 涨跌停/停牌 | `load_price_panel` | `backtest/panel_data.py` | `p = load_price_panel(s, e, cache_dir=".cache/x")` |
| 拿单只股票日线 | `load_raw_daily` / `load_daily` | `database/loader.py` | `load_daily("600519", s, e, adjust="qfq")` |
| 拿 PE / PB / 市值 / 换手 | `panel[...]` 或 `load_valuation` | 同上 | `p["pe_ttm"], p["pb"], p["total_mv"]` |
| 拿开高低收 | `panel["open"/"high"/"low"/"close"]` | — | `p["close_adj"]`（复权） |
| 拿涨跌停价 | `panel["limit_up"/"limit_down"]` | — | `p["limit_up"]`；NaN=无限制 |
| 判断能不能交易 | `panel["suspended"]` | — | `~p["suspended"]` |
| 拿指数点位 | `load_index_close` | `database/loader.py` | `load_index_close("000300.SH", s, e)` |
| 拿指数历史成分 | `index_member_panel` | `universe/pool.py` | 见 4.4 |
| 查某天的行业 | `industry_at` / `industry_pit_panel` | `database/industry.py` | `industry_at("600519","2024-06-28")` |
| 查行业清单 | `load_sw_l1` / `load_sw_member` | `database/loader.py` / `industry.py` | — |
| 查某天可交易的股票 | `tradable_at` | `database/master.py` | `tradable_at("2024-06-28")` |
| 查停牌记录 | `load_suspensions` / `is_suspended` | `database/status.py` | — |
| 查两融 / 北向 / 期货 / 期权 | `load_margin` 等 | `factors/market.py` | 见 10.4 |
| 查复权因子 | `load_factor` | `database/loader.py` | — |

### 10.2 选股与因子

| 我想…… | 用什么 | 在哪 |
|---|---|---|
| 建股票池 | `UniverseSpec` + `build_universe` | `universe/pool.py` |
| 只看某个行业 | `mask & industry_pit_panel(...).eq("医药生物")` | 见 4.2 |
| 按指数成分选 | `UniverseSpec(index_code="000300.SH")` | 同上 |
| 剔除 ST / 次新 / 低流动性 | `UniverseSpec(exclude_st=, min_listed_days=, min_amount=)` | 同上 |
| 按 PE/PB 筛 | 宽表布尔运算 | 见 4.3 |
| 算动量 | `c/c.shift(20)-1`（c=`close_adj`）或 `get_factor("mom_20")` | `factors/library.py` |
| 算波动 / 换手 / 振幅 | `pct_change().rolling(20).std()` / `rolling(20).mean()` | 见 5.3 |
| 看有哪些现成因子 | `list_factors()` | `factors/library.py` |
| 写自己的因子 | `@register("名字", ...)` | `factors/base.py` |
| 评估因子有效性 | `evaluate(get_factor("bp"), panel)` | `factors/evaluation.py` |
| 多因子合成 | `composite_score(panel, names, method=)` | `factors/composite.py` |

### 10.3 组合、回测、评估

| 我想…… | 用什么 | 在哪 |
|---|---|---|
| 从打分造目标权重 | `build_target_weights` | `portfolio/construction.py` |
| 改调仓频率 | `build_target_weights(rebalance="M"/"W"/"D"/10)` | 同上 |
| 改变加权方式 | `weighting="equal"/"market_cap"/"inv_vol"` | 同上 |
| 加择时 / 控总仓位 | `exposure_from_signal` + `apply_exposure` → `eng.run(exposure=)` | `factors/market.py` |
| 加风控 | `build_risk_manager({...})` → `eng.run(risk_manager=)` | `risk/portfolio_rules.py` |
| 跑回测 | `PortfolioBacktestEngine().run(panel, tw)` | `backtest/multi_engine.py` |
| 手动下单 | `Portfolio.buy/sell` + `SimulatedBroker` + `MarketRules` | 见 7.2 |
| 看回测报告 | `res.render()` | `analytics/result_report.py` |
| 算绩效指标 | `performance_summary(equity, bench)` | `analytics/performance.py` |
| 风格归因 / 残差 alpha | `build_style_panels` / `residual_alpha` | `analytics/style.py` |
| 导出交易记录 | `export_trade_log(...)` | `analytics/trade_log.py` |
| 画净值图 | `plot_performance(equity, bench, path)` | `analytics/report.py` |
| 聚宽风格 HTML 报告 | `build_jq_report(...)` | `analytics/jq_report.py` |
| 参数寻优 / 滚动窗口 | `optimizer/search.py` / `optimizer/walkforward.py` | `optimizer/` |
| 数据快照 / 回归基准 | `analytics/snapshot.py` | — |

### 10.4 市场级数据（两融 / 北向 / 期货 / 期权 / ETF）

```python
from factors.market import (load_margin, load_northbound, load_futures_basis,
                            load_option_pcr, market_signals,
                            exposure_from_signal, load_etf_basic, load_etf_prices)
```

> ⚠️ 这些数据都是**收盘后发布**的，用作信号时**必须 `lag≥1`**
> （`market_signals(..., lag=1)` 已内置）。
> 另外：它们的预测力已被检验过，**结论是基本没有** ——
> 见 `docs/已知问题与待办汇总.md` 2.13，接之前先读那一节。

---

## 十一、常见问题

**Q：`FileNotFoundError` / DuckDB 说 `No files found`？**
没有 `db/` 数据。数据由 `scripts/download_frozen_tushare.py` 等重建；
只跑纯合成测试不受影响（`python scripts/check_offline.py` 就是干这个的）。

**Q：面板加载要 8 分钟？**
首次正常。传 `cache_dir=".cache/panel_xxx"`，之后 <10 秒。
改了底层数据后面板缓存会按**数据指纹**自动失效，不会用旧数据。

**Q：`KeyError: 'trade_date'`？**
大概率是 loader 返回了**空表或没有该列**。守卫生效不了（只判 `.empty` 不够）
会导致 merge 直接炸。先 `print(df.columns)` 看一眼。

**Q：涨跌停是 NaN，是不是数据缺了？**
不是。**NaN = 该日没有涨跌幅限制**（新股上市初期、停牌复牌首日）。
按"无限制"处理，但要意识到这些日子不该随便交易。

**Q：没有 scipy，怎么算 Spearman？**
复用 `factors/evaluation.py::_corr`（先 `rank()` 再 Pearson）。

**Q：回测结果是不是就能拿去实盘？**
先看报告头部的三行声明（成交时点 / 门禁 / 已知缺陷），再看 `res.ledger_gap`
是否为 0。`same_close` 口径是**上界，实盘不可复现**。
再有，日线策略有物理天花板，见 `docs/日线局限与分钟级扩展痛点.md`。

**Q：提交前要做什么？**
```bash
python -m ruff check .            # CI 门禁
python scripts/run_tests.py       # 全部测试
python scripts/check_offline.py   # 无 db 环境下的离线门禁（必须）
```
**不要 push**，提交到本地即可。

**Q：想加一个新的数据源/字段？**
先看 `docs/数据结构说明.md` 与 `docs/数据字段说明.md`；
写 parquet 一律用 `database.storage.atomic_to_parquet`（非原子写会留下半截文件）。

---

## 十二、目录结构

```
D:\quant\
├── database/          数据层：只读原始层(frozen) + 派生层(cleaned) + 主表 + 行业 + 缺陷登记
│   ├── loader.py          单只/单日读取（load_daily / load_valuation / ...）
│   ├── master.py          证券主表三层过滤（时点正确的可交易池）
│   ├── industry.py        PIT 申万行业
│   ├── defects.py         已知数据缺陷**唯一登记处**（回测报告自动附注）
│   ├── storage.py         原子写 parquet（写数据必须用它）
│   └── config.py          路径与数据集登记
├── factors/           研究层：面板 + 因子 + 评估 + 门禁
│   ├── panel.py           load_panel（横截面宽表）
│   ├── library.py         18 个内置因子
│   ├── evaluation.py      IC / 分层 / 换手 / 衰减
│   ├── fundamental.py     PIT 财务因子
│   ├── market.py          两融/北向/期货/期权/ETF 消费方
│   └── gate.py            因子门禁（4 个独立条件）
├── universe/          股票池：历史指数成分 + 多维过滤
├── portfolio/         组合构建：选股 → 加权 → 调仓 → 目标权重
├── backtest/          回测：单标的 engine.py / 组合 multi_engine.py + 前视自检
├── execution/         执行：broker / market_rules / impact / setup（CLI 参数）
├── risk/              风控：单标的 manager.py / 组合 portfolio_rules.py
├── analytics/         分析：performance / style / attribution / report / trade_log
├── optimizer/         寻优：网格 / 随机 / 滚动窗口
├── joinquant/         聚宽 API 兼容层（写聚宽风格策略时用）
├── strategies/        策略代码（jq/ 下是聚宽移植过来的两个）
├── scripts/           可执行入口（见下表）
├── tests/             测试（`run_tests.py` 自动发现）
├── docs/              文档
└── db/                数据（不入版本库，约 7 GB）

产出目录：results/（回测产物）、.cache/（面板缓存）—— 都不入库。
```

**常用脚本**：

| 脚本 | 用途 |
|---|---|
| `scripts/portfolio_backtest.py` | 组合回测 CLI（最常用） |
| `scripts/factor_eval.py` | 因子有效性评估 |
| `scripts/multifactor_backtest.py` | 多因子选股完整流程（训练/测试分割） |
| `scripts/sector_rotation_backtest.py` | 板块轮动策略 |
| `scripts/jq_backtest.py` | 跑聚宽兼容层策略 |
| `scripts/semiconductor_rotation.py` | 逐决策 PIT 校验的**正确范例** |
| `scripts/validate_data.py --check <项>` | 数据校验（⚠️ 全量很慢，见 AGENTS.md） |
| `scripts/run_tests.py` / `check_offline.py` | 测试 / 离线门禁 |
| `scripts/daily_update.py` | 日常增量更新 |

---

## 附：本文档的验证方式

本文示例在 2026-09 用**本项目真实数据**跑通过：

- 第 1.2 节的完整示例：沪深300 池 → 医药生物行业 → 动量+EP 打分 → 月度调仓
  20 只等权，2023-01-01 ~ 2024-12-31，484 个交易日，产出 98 笔成交、
  5 个交易记录文件，对账通过（账目差额 0）。
  **示例的策略本身是亏的（−27.33%）—— 它只用于演示接口，不是策略推荐。**
- 第十章的速查表由一段自动化断言逐项核对（**表里列的每个函数都真实存在**），
  24 个章节的示例全部实跑通过。
- 表格里的列名、单位、函数签名全部取自实际返回值 / 源码，不是凭印象写的。

**写这份文档时顺手修掉了 5 个"一写就露馅"的问题**（都已加回归测试 / 自检）：

| # | 问题 | 症状 |
|---|---|---|
| 1 | `database.loader.to_qfq()` 没缩放 `pre_close` | 复权后同一行 `close` 是复权价、`pre_close` 是不复权价，自算涨跌幅与 `pct_chg` 差好几个百分点 |
| 2 | `industry_median_factor()` 的 `ts_code` / `code` 对不上 | **恒返回空字典**；且逐个读 7 万个 parquet，5 分钟跑不完（改 DuckDB 单日查询后 0.9 秒） |
| 3 | `backtest.panel_data.align_to()` 只对齐状态表 | `total_mv` / `pb` 等估值表在某只股票"有行情没估值"时少一列，下游 `build_target_weights(..., mv=...)` 抛 `ValueError: setting an array element with a sequence`，完全指不到原因 |
| 4 | `factors.panel.load_panel()` 同样不统一轴 | 同 3；已统一为"所有宽表同轴，缺数据补 NaN" |
| 5 | 面板缓存不校验非 close 表的形状 | 修了 3/4 之后，旧缓存仍会把少列的表喂给下游 —— 缓存现在会自检每张表与 `close` 同形状 |

> 这 5 个问题的共同教训和第九章第 4 条一致：**形状/口径不一致时要让它显式失败或返回
> NaN，不要留一个"看起来能跑"的中间状态。**
